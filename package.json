{
  "name": "ticketing-system",
  "version": "1.0.0",
  "description": "This is a **handoff bundle** from Claude Design (claude.ai/design).",
  "main": "index.js",
  "scripts": {
    "start": "http-server project/app -p 3000 -o \"TIXA Ticketing System.html\" --cors -c-1",
    "dev": "http-server project/app -p 3000 -o \"TIXA Ticketing System.html\" --cors -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.60.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
